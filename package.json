{
  "name": "install-all",
  "version": "0.0.1",
  "description": "small lib that loops a directory and performs a npm install on all application folders",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
